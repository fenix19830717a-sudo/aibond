# Aibond 项目总结与偏离检查

> 生成时间: 2026-06-22
> 项目目标: 企业人机协同路由平台 (aibond)
> 生产环境: https://aib2b.bond

---

## 一、项目架构总览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           MCP 客户端层                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────────────┐ │
│  │  TRAE IDE    │  │ Claude Code  │  │  其他 MCP 客户端               │ │
│  │  (HTTP模式)  │  │  (stdio模式) │  │                                │ │
│  └──────┬───────┘  └──────┬───────┘  └────────────┬───────────────────┘ │
│         │                 │                        │                     │
│         │  HTTPS / stdio  │                        │                     │
│         └─────────────────┴────────────────────────┘                     │
│                           │                                              │
│              ┌────────────┴────────────┐                                 │
│              │  aibond-mcp-server        │                                 │
│              │  (stdio / HTTP 双模式)    │                                 │
│              └────────────┬────────────┘                                 │
│                           │  WebSocket 长连接                            │
│         ┌─────────────────┼─────────────────┐                           │
│         │                 │                 │                           │
│  ┌──────▼──────┐  ┌──────▼──────┐  ┌──────▼──────┐                      │
│  │  用户浏览器  │  │  Agent SDK  │  │  平台后端    │                      │
│  │  (React)    │  │  (Python)   │  │  (FastAPI)   │                      │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘                      │
│         │                │                │                              │
│         └────────────────┴────────────────┘                              │
│                    wss://aib2b.bond                                      │
│                           │                                              │
│                    ┌──────▼──────┐                                       │
│                    │  SQLite DB  │                                       │
│                    └─────────────┘                                       │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 二、各模块现状

### 2.1 前端 (React + Vite + Ant Design)

| 组件 | 状态 | 备注 |
|------|------|------|
| `Chat.tsx` (1100+行) | 功能完整 | 消息渲染、文件上传、Session管理、WebSocket、@提及 |
| `Agents.tsx` | 功能完整 | Agent注册、Token创建、连接指南 |
| `Groups.tsx` | 功能完整 | 群组CRUD、成员管理 |
| `Workflow.tsx` | 功能完整 | 工作流列表 |
| `WorkflowEditor.tsx` | 功能完整 | ReactFlow可视化编辑器，5种节点类型 |
| `Login.tsx` | 功能完整 | JWT登录/注册 |
| `MainLayout.tsx` | 功能完整 | 侧边栏导航 |

**问题**:
- `package.json` 版本 `0.0.0`，未与项目版本同步
- `Chat.tsx` 1100+行，应拆分为子组件

### 2.2 后端 (FastAPI + SQLite)

| 模块 | 状态 | 备注 |
|------|------|------|
| `auth.py` | 功能完整 | JWT认证、注册、用户信息 |
| `agents.py` | 功能完整 | Agent注册、Token创建、心跳、列表(带owner过滤) |
| `groups.py` | 功能完整 | 群组CRUD、成员管理 |
| `messages.py` | 功能完整 | 消息发送(含群组权限验证) |
| `sessions.py` | 功能完整 | Session(任务会话)CRUD、消息、状态更新 |
| `workflows.py` | 部分完成 | 工作流定义、运行(仅创建实例，无节点执行逻辑) |
| `files.py` | 功能完整 | 文件上传下载 |
| `offline.py` | 功能完整 | 离线消息存储 |
| `websocket/manager.py` | 功能完整 | WebSocket连接管理、离线消息 |
| `main.py` | 功能完整 | 入口、路由、Agent WebSocket端点(650+行) |

**问题**:
- `main.py` Agent WebSocket 逻辑 650+行，应拆分
- 使用 SQLite 作为生产数据库
- `requirements.txt` 包含 redis 但未使用
- 后端版本 `0.1.0`，SDK 版本 `0.4.0`，不一致

### 2.3 Agent SDK (aibond-agent)

| 文件 | 状态 | 备注 |
|------|------|------|
| `mcp_server.py` | 功能完整 | stdio模式，10个MCP Tools，生命周期管理 |
| `mcp_http.py` | 功能完整 | HTTP模式(Streamable HTTP)，会话管理 |
| `client.py` | 功能完整 | WebSocket客户端，心跳，自动重连 |
| `agent_runtime.py` | 功能完整 | Skill注册表，规则处理，LLM集成框架 |
| `cli.py` | 功能完整 | CLI入口(connect/mcp子命令) |

**测试覆盖**: 5个测试文件，103个测试，全部通过

**问题**:
- `.mcp.json` 包含明文API Key (安全风险)
- `agent_runtime.py` 的 `_llm_process` 依赖外部 `llm_client`，无默认实现

---

## 三、偏离检查

### 3.1 与原始设计目标的偏离

| 设计目标 | 当前状态 | 偏离程度 |
|---------|---------|---------|
| 人机协同平台 | 功能完整 | 无偏离 |
| Agent通过MCP连接 | stdio + HTTP双模式支持 | 无偏离 |
| WebSocket长连接通信 | 已实现 | 无偏离 |
| 群组聊天 | 已实现 | 无偏离 |
| 任务分配(Session) | 已实现 | 无偏离 |
| 工作流编排 | 编辑器完成，执行引擎不完整 | **中等偏离** |
| 文件传输 | 已实现 | 无偏离 |
| 离线消息 | 已实现 | 无偏离 |

### 3.2 架构偏离

| 架构决策 | 原始设计 | 当前实现 | 偏离 |
|---------|---------|---------|------|
| Agent通信方式 | 手写脚本 | MCP Server作为通信层 | 已纠正 |
| MCP传输模式 | 仅stdio | stdio + HTTP双模式 | 增强 |
| 消息推送模式 | WebSocket推送 | Inbox轮询(Tool调用) | 符合MCP协议 |
| 认证方式 | 无认证 | JWT + API Key双认证 | 增强 |
| 数据库 | SQLite | SQLite | 未升级 |

### 3.3 代码质量偏离

| 指标 | 现状 | 目标 | 偏离 |
|------|------|------|------|
| 测试覆盖率 | SDK: 103个测试 | 应覆盖后端和前端 | **偏离** |
| 代码重复 | 根目录存在重复代码 | 应清理 | **偏离** |
| 敏感信息泄露 | API Key明文 | 应使用环境变量 | **偏离** |
| 版本一致性 | 前后端/SDK版本不一致 | 应统一 | **偏离** |
| 代码拆分 | main.py 650+行, Chat.tsx 1100+行 | 应模块化 | **偏离** |

---

## 四、安全风险

| 风险 | 位置 | 严重程度 | 修复建议 |
|------|------|---------|---------|
| API Key明文泄露 | `aibond-agent/.mcp.json` | **高** | 改为占位符，文档说明替换方式 |
| 测试账号密码明文 | `tests/test_platform.py` | **高** | 改为环境变量或占位符 |
| SECRET_KEY随机生成 | `backend/config.py` | 中 | 生产环境必须设置固定SECRET_KEY |
| SQLite用于生产 | `backend/database.py` | 中 | 建议迁移到PostgreSQL |

---

## 五、待办清单

### 高优先级

- [ ] 清理 `.mcp.json` 中的明文API Key
- [ ] 清理 `tests/test_platform.py` 中的明文密码
- [ ] 删除根目录重复的 `aibond_agent/` 和 `build/` 目录
- [ ] 统一项目版本号 (建议全部设为 0.4.0)

### 中优先级

- [ ] 拆分 `backend/app/main.py` 中的Agent WebSocket逻辑
- [ ] 拆分 `frontend/src/pages/Chat.tsx` 为子组件
- [ ] 完成工作流执行引擎 (`workflows.py` 的节点执行逻辑)
- [ ] 为后端添加单元测试
- [ ] 为前端添加单元测试

### 低优先级

- [ ] 迁移数据库到PostgreSQL
- [ ] 移除未使用的redis依赖
- [ ] 更新 `frontend/package.json` 版本
- [ ] 为 `agent_runtime.py` 添加默认LLM实现

---

## 六、部署状态

| 服务 | 状态 | 地址 |
|------|------|------|
| Web UI | 运行中 | https://aib2b.bond |
| Backend API | 运行中 | https://aib2b.bond/api |
| Agent WebSocket | 运行中 | wss://aib2b.bond/ws |
| MCP Server (HTTP) | 运行中 | https://aib2b.bond/mcp |
| MCP Server (stdio) | 本地启动 | `aibond-mcp --server ... --token ...` |

---

## 七、GitHub仓库

| 仓库 | 地址 | 最新提交 |
|------|------|---------|
| aibond-agent (SDK) | https://github.com/fenix19830717a-sudo/aibond-agent | `4544931` |
| aibond (主项目) | 未推送最新代码 | - |

---

## 八、核心设计决策回顾

### 8.1 正确的设计决策

1. **MCP Server作为通信层** — 从"手写脚本"纠正为"MCP通道化"
2. **Inbox轮询模式** — 解决了Tool Call同步 vs WebSocket异步的矛盾
3. **双传输模式** — stdio(本地) + HTTP(远程)覆盖所有使用场景
4. **TDD驱动开发** — 103个测试，7个bug被主动发现并修复
5. **JWT + API Key双认证** — 用户和Agent分别认证，权限清晰

### 8.2 需要改进的设计决策

1. **SQLite用于生产** — 高并发下会成为瓶颈
2. **工作流执行引擎不完整** — 只有编辑器，缺少执行逻辑
3. **缺少默认LLM实现** — Agent Runtime依赖外部LLM客户端
4. **版本管理混乱** — 前后端/SDK版本不统一

---

## 九、结论

项目整体**未偏离核心目标**，aibond作为企业人机协同平台的功能框架已经搭建完成，MCP集成、WebSocket通信、认证授权等核心机制均已实现并通过测试。

主要问题集中在：
- **安全**: 明文敏感信息需要清理
- **代码组织**: 部分模块过于庞大需要拆分
- **完整性**: 工作流执行引擎需要补充
- **一致性**: 版本号需要统一

建议下一步优先处理安全问题和代码组织问题，然后补充工作流执行引擎。
